// Re-export the shared Toast context implementation to ensure a single
// source-of-truth for `useToast` and `ToastProvider` across the app.
export { ToastProvider, useToast } from '@shared/contexts/ToastContext';

